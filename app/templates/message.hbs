

 {{#if isEditing}}
  <h2>{{model.question}}</h2>
  <form>
    <div class="form-group">
      <label for="question">Question</label>
      {{input id="model.question" class="form-control" value=model.question}}
    </div>

    <div class="form-group">
      <label for="body">Description</label>
      {{textarea id="body" class="form-control" rows="15" value=model.body}}
    </div>

    <button class="btn btn-primary" {{action 'save'}}>Save</button>
  </form>

 {{else}}
  <h3><p>{{model.question}}</p></h3>

  <p>
    {{model.body}}
  </p>
<button class="btn btn-primary" {{action 'edit'}}>Edit</button>
<button class="btn btn-danger" {{action 'delete'}}>Delete</button>
{{/if}}

<h2>Answers</h2>

  <!-- {{#each comments}}
    <p>{{text}}</p>
  {{/each}}

  <p>
    {{#link-to 'new-comment' this.id class="btn btn-default"}}Answer this question{{/link-to}}
  </p>

  {{outlet}} -->
